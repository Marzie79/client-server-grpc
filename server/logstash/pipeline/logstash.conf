input {
	rabbitmq {
		id => "rabbitmq_logs_1"
        host => "localhost"
        port => 5672
        vhost => "/"
        ack => false
        password => "guest"
	}
}

output {
   elasticsearch {
      hosts => ["127.0.0.2:9200"]
	  user => "elastic"
      password => "changeme"
      manage_template => false
      index => "%{[@metadata][index]}-%{+YYYY.MM.dd}"
   }
}